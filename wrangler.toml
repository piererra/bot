name = "bot"
main = "src/index.js"
compatibility_date = "2024-09-01"

# Create a KV namespace in the Cloudflare dashboard first
# (Workers & Pages -> KV -> Create namespace), then paste its ID below.
kv_namespaces = [
  { binding = "DATA", id = "50e1d0a1b12d4206b5e1e3c50db2068b" }
]
